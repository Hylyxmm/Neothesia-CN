# Neothesia（中文版）

本项目是基于开源项目 **Neothesia** 的汉化与功能改编版本。

## 项目来源

- **上游项目**：[PolyMeilex/Neothesia](https://github.com/PolyMeilex/Neothesia)
- **原作者**：PolyMeilex 及社区贡献者
- **上游许可证**：GNU General Public License v3.0（GPL-3.0）

Neothesia 是一个用 Rust 编写的跨平台 MIDI 可视化工具（类似开源版的 Synthesia），把 MIDI 文件中的音符显示为虚拟钢琴上落下的彩色方块，帮助用户学习钢琴。

本项目在上游基础上进行了**中文本地化**与**功能改造**，遵循 GPL-3.0 协议继续开源。

## 改造内容

相对上游版本，本分支共 4 个功能提交，改动集中在**五线谱（staff）预览渲染**这一全新模块：

### 1. 滚动五线谱预览（grand-staff preview） `acc5849`
- 在进度条与瀑布之间新增一条**滚动的大谱表（高音 + 低音谱表）预览**，使用 Bravura SMuFL 字体渲染谱号、符头、变音记号、符尾、拍号。
- **符干方向跟随 MIDI 轨道（手 / 声部）而非绝对音高**：右手轨永远朝上、左手轨永远朝下，即便手跨越中央 C 也保持一致。多轨按平均音高映射到高/低音谱表；单轨时回退为按中央 C 分割。
- 内置 **NotoSansCJK** 字体以支持中文界面文字，并打通 staff 渲染器、配置、菜单与 MIDI 解析所需改动。

### 2. 按实际拍号计算小节宽度 `09456e6`
- 上游小节宽度硬编码为 4/4（恒为 `4 * ppq`），导致 3/4、6/8、2/2 等文件的**小节线画错位置**，五线谱 / 辅助线小节与拍子错位。
- 改为按解析出的拍号计算：`ticks_per_measure = ppq * 4 * num / den`。4/4 不变；3/4 → 1440 ticks、6/8 → 1440 ticks、2/2 → 1920 ticks（ppq=480）。
- 新增 4/4、3/4、6/8、2/2 小节宽度的单元测试。

### 3. 符尾连接与装饰音 `5c4d465`
- **符尾连接（共享符尾 / beaming）**：同一声部、同一拍内的连续八分 / 十六分 / 三十二分音符共享横梁，而非各自带独立符尾。符尾组在一拍内按相同的时值间隔延伸。
- **装饰音（倚音 / grace notes）**：存储时值 ≤ 三十二分、且紧接一个较长主音的音符，以约 60% 大小画在主音符头左侧。按**时值**（而非间隔）判定，避免把滚动的琶音（短间隔、长持续）误判为装饰音。
- **尺寸调整**：谱表条带加宽 20%（`BAND_H` 166 → 199），线距缩小（`LINE_SPACING` 11.2 → 10），使上/下加三线音符能完整显示而非被裁切。

### 4. 稳定的八度移位方括号 + 谱表加宽 `edce2cb`
- 八度移位（8va/8vb/15ma/15mb）方括号原先绑定到"当前可见音符"：新音符滑入时方框**变大**、音符滚出时方框**缩小**——实时渲染痕迹明显。
- 改为基于**向可见窗口左右两侧各延伸若干小节**的音符列进行检测，方括号起止绑定到乐句**真正的首尾音符**（哪怕在画面之外），方框在完整生命周期内长度恒定；画面之外的部分由 scissor 裁剪。
- 方括号从"紧贴单个谱表边缘"改为贴整个谱表条带的**上下边缘**，远离被移位后的音符；谱表条带进一步加宽（`BAND_H` 199 → 220），为上下方括号预留空间。

### 5. 中文本地化
界面与说明文档的中文化。

> 完整改动细节可对照 git 提交历史。

## 改造执行

本汉化与改造工作由 AI 编程助手完成：

- **Claude Code**（Anthropic）
- **ZCode + GLM-5.2**

人工主导需求、验收与决策。

## 许可证

本项目继承上游的 **GPL-3.0** 许可证，详见 [LICENSE](./LICENSE)。

依 GPL-3.0 要求：
- 本项目保留上游原始 LICENSE 与版权声明；
- 所有源代码修改均以 GPL-3.0 继续开源；
- 任何再分发或衍生作品须同样遵循 GPL-3.0。

## 构建产物

`dist/` 目录下提供已编译的可执行文件（仅供便利，非上游官方构建）：

- `dist/linux/neothesia` —— Linux x86-64
- `dist/windows/neothesia.exe` —— Windows x86-64

如需自行构建，请参考上游项目的 CI 配置（`.github/workflows/`），在对应平台执行 `cargo build --release`。
